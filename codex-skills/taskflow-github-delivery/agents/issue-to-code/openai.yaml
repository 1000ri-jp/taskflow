interface:
  display_name: "Issue to Code"
  short_description: "Turn a scoped issue into a tested PR handoff"
  default_prompt: "Use $taskflow-github-delivery issue-to-code to triage and implement the scoped issue, verify affected behavior, and open a PR only within the authorized scope."
