interface:
  display_name: "PR Review"
  short_description: "Review a PR for bugs and missing verification"
  default_prompt: "Use $taskflow-github-delivery pr-review to inspect the PR and report findings and merge readiness. A review does not authorize merging."
