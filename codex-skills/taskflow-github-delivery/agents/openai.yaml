interface:
  display_name: "TaskFlow GitHub Delivery"
  short_description: "Coordinate TaskFlow issues, PRs and delivery status"
  default_prompt: "Use $taskflow-github-delivery for the requested TaskFlow and GitHub stage, preserving the existing scope and authorization."
