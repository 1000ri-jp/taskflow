interface:
  display_name: "Merge and Deploy"
  short_description: "Perform authorized merge and verify deployment"
  default_prompt: "Use $taskflow-github-delivery merge-deploy to perform the authorized merge or release, retain required CI gates, and verify the actual result. Review approval alone does not authorize either action."
